# Claim 6 evaluation

Status before formal run: `BLOCKED` (execution pending).

The branch is release-ineligible until:

- the formal `orx` run reaches a terminal state;
- every acceptance check passes;
- the independent checker exits zero;
- both negative controls fail for their intended reasons;
- raw JSON and checker output are copied into a descendant candidate artifact;
- the canonical Space page exposes code, inline numbers, raw links, command,
  seed, CPU/runtime, limitations, and source anchors.
